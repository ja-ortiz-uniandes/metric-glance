# Copy this file to .env and fill in your values. .env is gitignored.
#
# Your own (or other trusted) install_id, used to weight corrections higher
# during training. Find yours in the extension background console:
#   (await browser.storage.local.get("mgInstallId")).mgInstallId
# Comma-separate multiple trusted ids if needed.
MG_TRUSTED_INSTALL_IDS=
